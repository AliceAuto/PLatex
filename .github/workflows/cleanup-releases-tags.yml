name: Cleanup Releases And Tags

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag_prefix:
        description: Tag prefix to clean (empty means all tags)
        required: false
        default: ""

permissions:
  contents: write

jobs:
  cleanup:
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[cleanup-now]')
    runs-on: ubuntu-latest
    steps:
      - name: Delete all releases
        uses: actions/github-script@v7
        with:
          script: |
            const releases = await github.paginate(github.rest.repos.listReleases, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              per_page: 100,
            });
            core.info(`Found ${releases.length} releases`);
            for (const rel of releases) {
              core.info(`Deleting release ${rel.id} (${rel.tag_name})`);
              await github.rest.repos.deleteRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: rel.id,
              });
            }

      - name: Delete tags by prefix
        uses: actions/github-script@v7
        with:
          script: |
            const prefix = core.getInput('tag_prefix');
            const refs = await github.paginate(github.rest.git.listMatchingRefs, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'tags/',
              per_page: 100,
            });
            const tags = refs
              .map(r => r.ref)
              .filter(ref => ref.startsWith('refs/tags/'))
              .filter(ref => !prefix || ref.substring('refs/tags/'.length).startsWith(prefix));
            core.info(`Found ${tags.length} tags to delete`);
            for (const ref of tags) {
              core.info(`Deleting ${ref}`);
              await github.rest.git.deleteRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: ref.substring('refs/'.length),
              });
            }
